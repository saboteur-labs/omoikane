import type { ParsedCommand } from '../types.ts';

export async function handleRepo(command: ParsedCommand): Promise<void> {
  // repo init: initialise repo, run learning brief dialogue, smoke tests
  process.stderr.write('Not yet implemented: repo init\n');
}
